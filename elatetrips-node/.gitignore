node_modules/
dist/
.env
*.log
coverage/
.DS_Store

# Runtime photo uploads (disk mock-first storage) — anchored to the app root
# so it does NOT swallow the src/modules/uploads/ source module.
/uploads/
